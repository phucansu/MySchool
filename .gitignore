# Dependencies
node_modules/

# Environment / secrets (never commit real values)
.env
.env.local
.env.*.local
.env.production
.env.development
# Keep the template committed
!.env.example

# Vercel
.vercel/

# Dev-only scratch scripts
scratch/

# Logs
*.log
npm-debug.log*

# Unverified-license asset: kept locally, not distributed (see N-06).
# Replace with your own licensed hero image, then remove this line.
public/hero.png
.neon
